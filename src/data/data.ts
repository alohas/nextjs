import { Note } from "../utils/types";

// const notes: Note[] = new Array(15).fill(1).map((_, i) => ({
//   id: Math.random().toString(36).substring(2),
//   title: `Note ${i}`,
//   comment: `Lorem Ipsum of note ${i}`,
// }));

const notes: Note[] = new Array();

module.exports = notes;
